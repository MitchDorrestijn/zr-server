package org.han.ica.oose.boterbloem.layers.datalayer;

// @path("login")
public class LoginService {
    /*
    pseudo code klopt nog niet, shit uit me vodagone dus neem het met een korrel zout
    private UserDao userDao = new UserDao(//new DatabaseProperties())

    @POST
    @PRODUCES(Mediatype.APPLICATION_JSON)
    @CONSUME(Mediatype.APPLICATION_JSON)
    public Response login(LoginRequest login){
       User user = userDao.login(login.user, login.password)
        if (user == null) {
         return Response.status(Response.Status.NOT_FOUND).build();
         }
         Loginresponse token = new LoginResponse(user.token,user.user);
         return Response.status(200).entity(token).build();
         }

     */
}
